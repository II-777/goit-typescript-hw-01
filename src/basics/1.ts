// Оголошення різних змінних з відповідними типами та їх ініціалізація.
const age: number = 50; // number
const username: string = 'Max'; // string
const toggle: bollean = true; // bollean
const empty: null = null; // null
const callback = (a: number): number => { // function parameter
  return 100 + a;
};
